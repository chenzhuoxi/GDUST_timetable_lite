name: gdust_lite
description: GDUST 课表查看工具 Lite 版 - 纯展示，导入 JSON 即用
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.17
  intl: ^0.18.0
  path_provider: ^2.0.12
  file_picker: ^5.0.0
  home_widget: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
